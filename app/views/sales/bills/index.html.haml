= index_table(collection: collection, mform: true, icon: 'dollar-sign', text: 'Comprobantes de venta')

= modal("#{icon('fas', 'cogs')} Configurar campos", "config_modal", "config_modal_body", "big") do
	.modal-body
		= yield

= modal("#{icon('fas', 'cogs')} Configurar campos", "sended_state_modal", "sended_state_modal_body", "big") do
	.modal-body
		= yield
