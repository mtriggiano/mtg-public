.index
	.index-title
		.principal
			#{icon('fas', 'chart')} Reporte
	#bmenu.mx-4.d-flex.justify-content-between.align-items-center
	.index-body.table-responsive
		%table#paginated_table.table{data: {source: bank_accounts_path(format: :json, view: 'general')}}
			%thead.thead-light
				%tr
					%th.actions{scope: 'col'} Cheque
					%th.actions{scope: 'col'} Fcha. Vto.
					%th.actions{scope: 'col'} Fcha. Emisión
					%th.actions{scope: 'col'} Cheque
					%th.actions{scope: 'col'} Banco
					%th.actions{scope: 'col'} Proveedor
					%th.actions{scope: 'col'} Estado
					%th.actions{scope: 'col'} Observación
					%th.actions{scope: 'col'} Importe
					%th.actions{scope: 'col'} Pagos
					%th.actions{scope: 'col'} Saldo
			%tbody.table-bordered

	= table_script(DatatableConstants::Reports::BankAccount.merge("actions" => ""))

