= index_table(mform: true, collection: collection, text: "Cobros a clientes")

= modal("#{icon('fas', 'cogs')} Configurar campos", "config_modal", "config_modal_body", "big") do
	.modal-body
		= yield