= index_table(collection: collection, icon: 'hand-paper', text: "Solicitudes")

= modal("#{icon('fas', 'cogs')} Configurar campos", "config_modal", "config_modal_body", "big") do
	.modal-body
		= yield

