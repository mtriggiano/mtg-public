.col-10.bg-white.p-4.shadow
	%h3.text-muted
		Configuración de la compañía
	%hr
	#config_fields
		- view = @view || params[:view]
		- if ['general', 'location', 'anmat', 'accountant', 'sale_points', 'stores', 'banks', 'cards', 'departments'].include?(view)
			= render "/companies/config/#{view}"