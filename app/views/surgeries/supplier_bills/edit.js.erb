<% simple_nested_form_for supplier_bill do |f| %>
	<% @builder = f %>
<% end %>
<% if params[:params_ids] %>
	$(".parent-table tbody").html("<%= j render '/surgeries/shared_details_form', {f: @builder, colspan: 10} %>");
<% end %>
$(".cbte_tipo").html("<%= j render 'cbte_tipo', f: @builder %>")
