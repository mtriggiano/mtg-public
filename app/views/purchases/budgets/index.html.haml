= index_table(icon: 'file-import', collection: collection, text: "Presupuestos")

= modal("#{icon('fas', 'cogs')} Configurar campos", "config_modal", "config_modal_body", "big") do
	.modal-body
		= yield
