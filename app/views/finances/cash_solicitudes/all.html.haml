= index_table(mform: true, icon: 'dollar-sign', text: "Solicitudes de fondos", jpath: all_cash_solicitudes_path(format: :json) , path: new_external_cash_solicitudes_path(), mtarget: "#new_cash_solicitude_modal", collection: cash_solicitudes, all_perms: true )

= modal("#{icon('fas', 'plus')} Solicitud de fondos para agentes externos", "new_cash_solicitude_modal", "new_cash_solicitude_body", "big") do
	.modal-body
		= yield

= modal("#{icon('fas', 'edit')} Solicitud de fondos para agentes externos", "edit_cash_solicitude_modal", "edit_cash_solicitude_body", "big") do
	.modal-body
		= yield
