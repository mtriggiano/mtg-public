= index_table(icon: 'hand-paper', collection: collection, text: "Solicitudes")

= modal("#{icon('fas', 'cogs')} Configurar campos", "config_modal", "config_modal_body", "big") do
	.modal-body
		= yield
